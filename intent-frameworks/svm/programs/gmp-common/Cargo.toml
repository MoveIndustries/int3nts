[package]
name = "gmp-common"
version = "0.1.0"
edition = "2021"
description = "Cross-chain GMP message encoding/decoding for the int3nts protocol"

[lib]
name = "gmp_common"

[features]
default = []
